apiVersion: apps/v1
kind: Deployment
metadata:
  name: shellustad-frontend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: shellustad-frontend
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
  template:
    metadata:
      labels:
        app: shellustad-frontend
    spec:
      containers:
      - name: shellustad-frontend
        image: 798704874888.dkr.ecr.ap-southeast-1.amazonaws.com/shellustad-frontend:1.0.10
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 3000
        envFrom:
        - configMapRef:
            name: frontend-config
